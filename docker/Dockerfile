FROM amazoncorretto:17
WORKDIR /app
ENV ENV= \
    CLIENT_ID= \
    CLIENT_SECRET= \
    MONGO_URI=
ARG JAR_FILENAME
COPY $JAR_FILENAME ./app.jar
ENTRYPOINT ["sh", "-c", "java -DENV=$ENV -DCLIENT_ID=$CLIENT_ID -DCLIENT_SECRET=$CLIENT_SECRET -DMONGO_URI=$MONGO_URI -jar app.jar"]
